import 'package:flutter/material.dart';

class GremioScreen extends StatelessWidget {
  const GremioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Gremio Screen', style: TextStyle(color: Colors.white, fontSize: 24)));
  }
}
